# Verifying-Paymaster
Live Node Connectivity The RPC Endpoints panel accepts your actual Alchemy, Infura, or QuickNode Mainnet URLs with API keys. Hit Test Connection and it fires a real eth_blockNumber + eth_gasPrice JSON-RPC call — the live block number and base fee will populate the dashboard from the actual chain.
